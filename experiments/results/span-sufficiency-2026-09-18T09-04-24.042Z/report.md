# R43 independent sufficiency result

Raw span selector:16/16 exact positive selections,12/12 GENERATE controls. The second Noul at predeclared0.9 threshold emits8/16 positives, zero false emissions. Required>=14/16 emission gate FAILS. No runtime addition.

The second judgment is conservative even for literal repeats: correct quoted phrase receives0.66; correct room correction0.82; version0.81–0.83. It does not improve raw choice accuracy on this dataset, but adds a call and false fallback. No evidence here that broad absolute sufficiency is a useful gate. Do not lower its threshold on this test and count a retrospective pass.

A limitation matters: all fixture messages are represented as user role. Real history also contains assistant errors, and source candidates can include those errors. Before assuming perfect raw-selection precision generalizes, test mixed-role contradictory history, stale assistant claims and partial-span traps. The raw selection route remains a candidate; rejecting this Noul wrapper does not reject the underlying12/12 and16/16 extraction observations.

Usage accounting: per-row usage in results.json is FIRST selection request only; full combined use is in trace.jsonl. Total input across all selection and verification calls is34,556. Do not cite first-stage-only usage as total pipeline cost. Reported row ms includes both calls. Candidate orders/repeated cases are correlated, not independent benchmark tasks.
