# Obsolete Linux Chrome VM

Do not use this image. Jot’s browser runtime is **local headed Chrome**
(`channel: 'chrome'`, shared profile, CDP). This Docker Chromium + Xvfb stack
was an experiment for Google login and was dropped as too heavy.

See `docs/linux-chrome.md`.
