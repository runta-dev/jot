# Browser runtime

Default is **local headed Chrome** with a shared Jot profile. That is the
light path for Google sign-in on this Mac. `npm run dev` does not build a
Linux image.

`vm/linux-chrome` is obsolete and should not be started.
