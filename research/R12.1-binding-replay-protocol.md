# R12.1 — repair candidate composition, not the model outputs

Preregistered after auditing R12 traces. Replay all 16 cases using the exact cached selected senses, noun distributions, link distributions and adjective suggestions. No new lexical word or reference answer is added. Additional API usage covers only whole-sentence comparison.

Changes:

1. Retain four patient nouns instead of two; task was already present but pruned in procrastination.
2. Resultative primary arguments include realized patient NPs, not only spans copied from the request. This makes sort items into categories available instead of forcing sort the desk into categories.
3. Render unquoted, user-owned first-person references in second person; quoted/ambiguous ownership is rejected for this reference path rather than silently reassigned. Preserve the original request in traces.
4. Supply articles/count variants for bare count nouns used as source references.
5. Do not offer into-result constructions unless the selected sense definition expresses partition/classification/transformation; this is a declared conservative lexical heuristic, not a universal English grammar.

Keep all 16 cases, final comparison instructions and NONE. Bound <=4,000 variants per case. Report newly incurred cost separately from cached preparation. Gate is unchanged from R12 (>=9/12 development, >=3/4 previously fresh cases); the reused fresh set is now a regression set, so a later genuinely new test is still required before integration.
