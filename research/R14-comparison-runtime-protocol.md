# R14 — qualified comparison operator at the runtime boundary

Preregistered before live HTTP/browser tests. Package the R13.1 catalog as a fixed attributed snapshot; no runtime WordNet/QUDT downloads. Keep default character chat unchanged. An explicit `mode: comparison` request and `?experiment=compare` browser preview use separate local chat storage.

Runtime changes requiring qualification: MISSING entity options; reject same/overbudget entity spans; skip the redundant group pass when an explicit-property candidate set fits the API; bounded abort-aware transport; unknown/missing statuses distinct from EOS; no private conversation logging.

Live HTTP cases: three direction reversals/false suggestions, a viscosity reversal, a density regression, an unspecified material pair, missing entities, a non-comparison greeting, invalid mode/body and cross-origin requests. No-input tests must not invoke semantic inference beyond entity identification. Unit tests verify cancellation halts subsequent calls and provider validation rejects invalid options.

Browser: verify complete result, unknown result, missing-entity prompt, stopping, new chat/history and isolation from default storage; inspect actual rendered desktop/mobile layout. This preview is single explicit-question comparison, not validated multi-turn entity updating or general chat. Any unsupported follow-up is a documented boundary, not a successful answer.

No goal-completion claim from this component. Its runtime result only supports further integration into a broader pure-Jev semantic chat architecture.
