# R13.1 — scientific quantity vocabulary and explicit-property fast path

Preregistered before inference. Replace the broad WordNet bank with pinned QUDT quantity kinds plus the complete WordNet generic-property subtree (root 04923519). This root is selected as an ontology class, not by injecting successful answer words. Prefer QUDT when an exact label overlaps. The narrower physical_property subtree was inspected and rejected because it omits density/hardness in WordNet's hierarchy.

Use the same entity/property/relation procedure. When a property name is explicitly requested in a syntactic property position (e.g. in hardness, higher density, density of), compare only matching entries. Generic words such as property/quantity do not trigger this shortcut. Otherwise scan the complete combined bank. Definitions are generic lexical/quantity descriptions; no entity-specific comparison answer is added.

Repeat all ten R13 prompts, plus cork/water, helium/air, ethanol/water boiling point, and two unspecified alloy identifiers without an explicit no-data disclaimer. Assess at least 10/11 factual comparisons and all three unknown controls correctly, with lower mean input cost than R13. This remains a typed-comparison component, not completion of open chat.

QUDT source commit 75e5ef6a3bde9a01760007d148c6d52409f6a067, CC BY 4.0 attribution QUDT.org. WordNet license retained. Report exact-property shortcuts and remaining full scans separately.
