# R15.9 bounded lookahead diagnostic

Preregister before calls. Source R15.7 traces and active vocab. Cases: both creative states with prefix ending `Finally`, plus first Chinese rerank state with a nonempty prefix. Deterministic selection, no authored answer candidates. Retain top three non-END continuations from recorded distribution, extend each three selection steps using the same 240-word groups, top4 proposals/group, deterministic morphology, rendered continuation choice. Preserve full actual conversation and prefix; EOS stops branch. Final Choice among three resulting full prefixes, normal and reversed order.

Compare chosen first branch with original greedy first branch and assess grammatical viability/semantic consistency of actual lookahead fragments. At most 3 cases × 3 branches × 3 steps × 2 requests plus six final judgments. Raw trace and usage/elapsed saved. Single run, historical conditional states, not general-chat acceptance. No probability-product sequence scoring. Branches serial for simple budget accounting; do not infer production parallel latency. If short lookahead still prefers malformed story fragment, reject this depth/selector; do not silently increase depth or inject the missing subject.
