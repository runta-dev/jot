# R15.3 paired end-to-end representation ablation

Preregistered before measurements. Same first two development conversations; two separate runs: full continuation and appended fragment. Both use the R15.2 generic grounding instruction only in the final selection question. Vocabulary, state, proposals, budgets and stopping remain R15.1. Deduplicate candidates by rendered output, retaining first occurrence in both arms. Each arm includes only its own actual generated assistant messages. No reference answers supplied.

Run full then fragment, one sample per arm; time-order and model stochasticity are limitations. Do not infer statistically robust superiority from four turns. Gate for further evaluation remains all four adequate on this diagnostic, followed by remaining development categories; no production acceptance. Preserve all requests and responses. Evaluate task correctness, readability, contextual coherence, useful completeness separately; report per-turn latency/tokens. If both fail, inspect complete trajectories instead of adding more domain-specific handlers.
