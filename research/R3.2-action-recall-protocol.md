# R3.2 — retain two actions before pruning

Preregistered before collecting responses. Same localizer and proposal alphabet as R3.1. Retain the two highest-probability action hypotheses (including unchanged if present) instead of only the selected action. Original is always offered, and generic repeated-space collapse is always offered for final judgement, never blindly applied. Keep the top two locations per retained action. This should recover the two previously lost corrections without sacrificing unchanged controls.

If >255 proposals, split into groups of <=200 edits plus original; then compare group winners plus original. No new model or reference-based candidate construction. Test all 16 earlier cases as regressions and 8 new frozen probes. Do not refer to earlier cases as held-out.

New probes: Good mornng.; It is is working.; We was late.; That is double-space fine.; Where is it.; plus unchanged Do not change this., print("x y"), and The fee is $2,040. Gold values remain local.

Gate: >=7/8 new probes, all three new correct controls unchanged, no worse than 14/16 previous cases, and lower mean input cost on original eight than exhaustive R3. Passing only permits a generation-output editing experiment, not production promotion or a claim of conversational generation.
